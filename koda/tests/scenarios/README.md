# Test Scenarios

Each JSON file contains a test case with:
- `input`: User message (German or English)
- `expected_agent`: Which agent should handle this
- `must_contain`: Keywords expected in the response
- `must_not_contain`: Shame-reinforcing patterns to flag
